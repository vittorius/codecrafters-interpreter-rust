/*  TODO: replicate Expr hierarchy using trait Expr, classes that implement it as expr types,
and Box<dyn Expr> for self-references. Also, use StringVisitor (a visitor that returns a string
and is without generic params to overcome the dyn-compatibility)
*/
